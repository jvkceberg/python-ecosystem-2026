---
// Tip / warning / info box with an SVG icon (no emoji). Colors come from
// global tokens so dark mode works automatically.
import Icon from './Icon.astro';

interface Props {
  type?: 'tip' | 'warning' | 'info';
  title?: string;
}

const { type = 'tip', title } = Astro.props;
const defaults = {
  tip: { icon: 'lightbulb', heading: '알아두면 좋아요' },
  warning: { icon: 'triangle-alert', heading: '주의하세요' },
  info: { icon: 'info', heading: '참고' },
} as const;
const icon = defaults[type].icon;
const heading = title ?? defaults[type].heading;
---

<aside class={`callout ${type}`}>
  <p class="callout-title"><Icon name={icon} size={17} /><span>{heading}</span></p>
  <div class="callout-body">
    <slot />
  </div>
</aside>

<style>
  .callout {
    margin: 1.2rem 0;
    padding: 0.9rem 1.1rem;
    border-radius: 12px;
    border: 1px solid;
  }

  .callout.tip {
    background: var(--tip-bg);
    border-color: var(--tip-border);
  }

  .callout.tip .callout-title {
    color: var(--tip-ink);
  }

  .callout.warning {
    background: var(--warn-bg);
    border-color: var(--warn-border);
  }

  .callout.warning .callout-title {
    color: var(--warn-ink);
  }

  .callout.info {
    background: var(--info-bg);
    border-color: var(--info-border);
  }

  .callout.info .callout-title {
    color: var(--info-ink);
  }

  .callout-title {
    display: flex;
    align-items: center;
    gap: 0.45rem;
    margin: 0 0 0.35rem;
    font-weight: 700;
    font-size: 0.95rem;
  }

  .callout-body :global(p) {
    margin: 0.3rem 0;
  }

  .callout-body :global(p:first-child) {
    margin-top: 0;
  }

  .callout-body :global(p:last-child) {
    margin-bottom: 0;
  }
</style>
